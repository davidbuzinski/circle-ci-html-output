version: 2.1
orbs:
  matlab: mathworks/matlab@dev:v1-rc

jobs:
  build:
    machine:
      image: ubuntu-latest
    steps:
      - checkout
      - matlab/install
      - matlab/run-command:
          command: ver
      - matlab/run-tests:
          source-folder: code
